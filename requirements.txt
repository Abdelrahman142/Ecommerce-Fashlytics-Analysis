# Core analytics stack (Phase 3-6)
ijson==3.5.1          # streaming JSON parse of the 82.7 MB raw file
pandas==3.0.5         # ETL tabular processing / wide-pivot
pyarrow==24.0.0       # parquet I/O for the processed layer
psycopg[binary]==3.3.4 # PostgreSQL driver (COPY bulk load + dashboard queries)

# Dashboard (Phase 7)
streamlit==1.61.1
plotly==6.9.0

# Dev / testing
pytest==9.1.1
